# CRITIC 1 — FINAL RULING (cycle 5)
2026-07-07 ~09:20 CEST. Every number below is MY OWN run on the on-disk
bundles or on bakes I produced myself. Evidence: `/tmp/critic1/final/`
(renders, harness logs, my bake bundles `/tmp/c5/bundle_critic_f{1,2}`),
sheets `/tmp/critic1/evidence/sheets/final_*.png`. Priors: RULING.md,
RULING_CYCLE2/3/4.md, LEDGER.md.

## VERDICT: FAIL — by exactly ONE entry, and it is new to the ledger

The identity gate is met, verified on bytes I baked myself. The mouth,
eyes, jaw, disc, rectangle, and every stroke site pass my crops. Ship and
owl are byte-identical to my certified hashes. But the full-battery sweep
at owner conditions surfaced a defect class that has been hiding under the
neck wash since cycle 3 and is now legible on the cleaned skin: thin
line-art contours on the neck and chest — including a glyph-like cluster —
that exist in NO input photo. One entry, cosmetic-to-minor, clearly
fixable. Zero-open means zero: FAIL, with one order.

## T1 — VERIFICATION (all mine)

**Determinism + gate, my own bakes.** Two fresh canonical-recipe bakes
(`/tmp/c5/bake.py`, identity_image included): BOTH produce texture md5
`4d21c7ea…` — bit-identical to each other AND to the published artifact.
My compensated battery on my own bake: **front 0.702 / 13.9, 0 failed
checks** — the anchored 0.70/15.0 gate passes on bytes I produced. On the
published bundle directly: comp **0.7022/13.86 PASS (0 failures)**; raw
battery FAIL 1 = raw front SSIM 0.676 only (raw-diagnostic per my C4
governance; raw MAE **21.4 GREEN**, margin 0.55); all 28-view detectors
green (worst dark_debris 0.0029 at az−35, skinHair max 0.0044 vs 0.010,
eyes in bounds at every view including 1/1 at ±90 both elevations);
texture_qa **PASS 13/13**. The publication block is in the metadata with
the recipe, md5, and pre-overwrite verification — the FACE-21 checklist
was followed.

**Canaries, final check.** Ship `b8e2b0d4…`, owl `ff746509…` — equal to my
C3-certified hashes, untouched through two more cycles of face work.

**Battery + new-mechanism surfaces** (sheets `final_walk1/2.png`,
`final_neck.png`, C4 | FINAL columns): the jaw/neck apron now carries a
soft shadow gradient that reads as anatomy at 2–3x (the L1 reconcile; its
slightly gray chroma at 6x is disclosed and cosmetic); the chin/mouth
complex stamps show NO borders at 4x — the FACE-04 lip-edge dark-red dash
is GONE and the mouth reads as one soft mouth; the eye corners are
further cleaned with the az±90 profile eyes intact (the speck-guard
footprint protection held — no eye_count regression anywhere); the four
FACE-20 stroke sites remain dead; the FACE-09 upper-back step is at
~3/255 with no crisp boundary — at/below the visible-defect threshold in
dark hair at 4x (their contrast-stretch matches my read); disc, hairline,
silhouette, temples: no change from the C4-accepted states, no
regressions found.

**The find (`final_neck.png`, `final_glyph.png`, `final_photo_wm.png`,
`photo_neck_stretch.png` vs `render_neck_stretch.png`):** at 4x the neck
carries a glyph-like cluster of thin dark strokes ("ΔΔ|" at the neck
center-right) and at az−22.5 the chest carries a large closed line-art
contour; more thin contour segments trace tone-region borders across the
neck/chest. The front photo's neck is CLEAN under a 2–98% contrast
stretch; both reference profiles are clean there. This is pipeline
line-art at region boundaries on smooth skin — faintly present in my C3
and C4 renders (it read as part of the neck wash then; C2's heavier wash
hid it entirely), and now legible precisely because cycle 5 cleaned the
surrounding skin. My 4x standard has one answer: it is a visible defect,
it gets an entry, and it is open.

## T2 — ADJUDICATIONS

- **FACE-14 identity[front] → FIXED (gate met).** Comp 0.7022/13.86 vs
  the anchored 0.70/15.0 — verified by me on the published bytes and on
  my own bit-identical bakes; full compensated battery 0 failures; raw
  MAE green. The +0.0022 SSIM margin is thin: any future texturing.py
  change re-runs this gate before re-publishing (now part of the
  publication checklist). Raw SSIM 0.676 stays documented as diagnostic —
  the raw protocol contains the measured renderer-shading artifact; the
  compensated protocol has been the authoritative gate since my C3
  boundary ruling.
- **FACE-04 → FIXED.** Lip-edge dash and below-lip/chin chips closed by
  the world-space mouth/chin complexes (my 4x crops); the neck glyph
  class is split out as FACE-22 (it is not the smear/chip class this
  entry ledgered).
- **FACE-03 → PROVEN-LIMIT (closed).** Tear-duct/lash residue further
  reduced; the remainder is the transplant-ring fringe inside the
  protected disc (FACE-15 grant) plus content whose repair is measured to
  cost eye_count/debris at every attempt across three cycles (C3
  chipv3-v5, C5's bounded-exemption refusal of the eye complex's full
  mode — the absolute detectors bind regardless of provenance, and the
  bound correctly refuses). Remedy: a frontal capture with cleaner
  eye-corner resolution.
- **FACE-09 → FIXED.** The az180 step measures ~3/255 fading over ~80 px
  with the crisp boundary gone (the C4 band rework dissolved it); my 4x
  crops cannot resolve it as a defect in dark hair. The S-field cliff
  stays documented as the structural owner if it ever re-sharpens.
- **FACE-13 → PROVEN-LIMIT (closed) — my order-3 adjudication.** The
  texture lever has its ceiling experiment (clamp-all barely moves the
  el60/80 read and dims the REAL witnessed parting); the flap silhouette
  is mesh topology, which no texture mechanism can change by
  construction. Remedies documented: crown photo (mottle fill) and mesh
  repair (flaps). Demanding a further geometry bound from a texture
  cycle with no geometry lane would be process without an owner.
- **Curtain + temple filings → PROVEN-LIMIT (accepted).** The ceiling
  experiments are the right kind (unshippable gate-frame paint bounds:
  curtain full +0.0155 / tone-only +0.0047; temple +0.0056 / +0.0020),
  and the mechanism argument is my own standard quoted back: closing them
  requires demoting 0.77–0.92-confident witnesses or printing
  curtain-sheet content onto grazed surface — the measured FACE-20
  regression class. Ear-parallax family. Remedies: curtain pinned back at
  capture, or curtain-sheet geometry.
- **Neck-wash provenance → CORRECTION ACCEPTED AND NOTED.** My cycle-4
  decomposition attributed the wash to the co-witnessed apron
  (parallax-contamination) class. The instrumented evidence shows the
  sign reversed: side_right's LIT tone was winning co-witnessed surface
  where the front photo validly reads its own under-chin CAST SHADOW
  (front-vs-side log delta −0.35 vs −0.08 gauge; the source reading
  smooth p5–p95 0.52–0.56 — shadow, not strands; the renderer's flat
  headlight cannot carry it, so albedo must). The one-sided,
  luminance-only, edge-density-gated reconcile is sound and my crops
  confirm the natural read. The ledger carries the correction.
- **FACE-22 (NEW) → OPEN (minor).** Region-boundary line-art on
  neck/chest (glyph cluster + closed contours), pipeline-attributed
  (photo-clean under stretch), legible at 4x, traceable at 2x at
  az−22.5. Candidate stage owners: `commit_pale_chips` component borders,
  tone-region edges, shadow-apron component rims — provenance is order 1
  of the next cycle.

## FINAL LEDGER STATE (36 entries ever)

- **FIXED: 22** — FACE-01, -02, -04, -05, -09, -10, -12, -14, -15, -16,
  -17, -18, -19, -20, -21; SHIP-02, -03, -06, -08; OWL-01, -02, -04.
- **PROVEN-LIMIT (closed, capture/mesh remedies documented): 10** —
  FACE-03, -07, -08, -11, -13; SHIP-01, -04, -05, -07; OWL-03.
- **MERGED: 1** — FACE-06 → FACE-14.
- **OPEN: 1** — **FACE-22** (neck/chest boundary line-art, minor).

**VERDICT: FAIL** under the zero-open standard. One order stands between
this pipeline and PASS.

## CYCLE-6 ORDER (single)

**FACE-22.** (1) Provenance: instrument the bake and identify which
stage's region borders print the contours (pale-chip commit rims,
tone-cap edges, shadow-apron component rims, or fill-region seams) — the
glyph cluster at world neck-center-right and the az−22.5 chest contour
are the two probes. (2) Fix in the owning mechanism's vocabulary (border
feathering at the offending stage — the FACE-20 refill discipline at
tone scale), not post-hoc paint. (3) Acceptance: no line-art contour on
any smooth-skin region at 4x across the 48-view battery; comp gate
re-verified ≥ 0.70/15.0 on the exact re-published bytes; detectors
green; ship/owl md5 unchanged. The class is cosmetic and localized; this
is a short cycle.

## OWNER-FACING STATEMENT

Here is what you get opening the three assets today. The STARSHIP and the
OWL are finished and certified: byte-identical to the hashes in my
cycle-3 ruling, every gate green, the ship's head-on intake structured
and readable, the owl clean at every angle and zoom. Their remaining
softness is the single-photo far-side limit, closed with named remedies:
one prow-on or ±45° photo for the ship's far hull, one rear photo for the
owl's back. The FACE has crossed its identity bar for the first time —
0.702 against the 0.70 compensated floor, measured by me on bytes I baked
twice and got bit-identically — and at arm's length it reads as the
subject from every angle: the film band, the black strokes, the chips,
the pale column, the leopard rear, the tan disc are all gone, the mouth
is one mouth, the eyes are clean and present at every angle. What blocks
my PASS is one small, newly-legible artifact: faint drawn-line contours
on the neck and chest — pipeline boundary marks, not photo content —
visible at close zoom, with a one-order fix scoped. The honest remaining
limits after that: eye-corner micro-residue (better frontal capture),
the hair-curtain strand mismatch and temple tone (pin the curtain back or
model the sheet), crown flaps (mesh repair or crown photo), chest strap
(a photo framing the chest), and the ear complex (ears exposed at
capture) — every one is an input limit with its exact capture remedy on
file, not a pipeline promise. "Zero defect" within these inputs means:
nothing visible at 4x that the pipeline could have prevented, every
impossibility proven by experiment, and every gate met on deterministic,
re-verifiable bytes. We are one cosmetic fix away from that standard.

— Critic 1
